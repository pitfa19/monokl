name: Publish disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: PyPI publishing is disabled for Monokl migration
        run: |
          echo "Publishing to PyPI is intentionally disabled."
          echo "Re-enable only after Monokl release credentials and policy are established."
